project_id = "ophnjqjmxeohbyydxnlg"

[db]
# Matches the remote project's Postgres major version (17.6 at dump time).
major_version = 17

[api]
# Schemas exposed via PostgREST. Add "graphql_public" here only if you start
# using pg_graphql — currently unused.
schemas = ["public"]

[storage]
# Default 50 MB is fine; bump here if PDFs grow past that.
file_size_limit = "50MiB"

[auth]
# fin.catipult.ai is the live domain; tax-engine-app.netlify.app was the
# legacy Netlify subdomain and auth emails used to link there.
site_url = "https://fin.catipult.ai"
additional_redirect_urls = ["https://fin.catipult.ai/**", "https://tax-api.catalogshub.com/**", "http://localhost:5173/**", "http://localhost:3737/**"]

[auth.email]
enable_confirmations = false
